@model IEnumerable<MediaNoche.Models.Excersize>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<div class="Extra"  style="display:inline-block">
    <p>
        @Html.ActionLink("הוסף תרגיל", "Create")
    </p>
</div>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Video)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Video)
            </td>
        <td>
            @Html.ActionLink("פרטים", "Details", new { id = item.ID })
            <div class="Extra"  style="display:inline-block"> </div>|
            @Html.ActionLink("ערוך", "Edit", new { id=item.ID }) |
            @Html.ActionLink("מחק", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>


