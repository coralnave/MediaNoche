@model IEnumerable<MediaNoche.Models.Evening>

@{
    ViewBag.Title = "הצגה";
}

<h2>הצגה</h2>
<div class="Extra" style="display:inline-block">
    <p>
        @Html.ActionLink("הוסף ערב", "Create")
    </p>
</div>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EveningLessonID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Concept)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EveningLessonID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Concept)
        </td>
        <td>
            @Html.ActionLink("פרטים", "Details", new { id = item.ID })
            <div class="Extra" style="display:inline-block">
                |
                @Html.ActionLink("עריכה", "Edit", new { id = item.ID })|
                @Html.ActionLink("מחק", "Delete", new { id = item.ID })
            </div>
       
        </td>
    </tr>
}
</table>